# API Documentation

This page has moved to **[API](API)**.

The two pages had drifted into separate, partly contradictory copies of the same reference. [API](API) is the maintained one — it is kept in step with the code and covers the current endpoints.

This stub stays in place so existing links and bookmarks keep working.

**→ [Go to the API reference](API)**
