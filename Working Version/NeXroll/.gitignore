# Local repo .gitignore override to allow production/ to be tracked
# Parent folder may ignore 'Working Version/' which can cause production/ to be ignored.
# Explicitly allow production directory at repo root
!/production/

# Keep common defaults (optional, add more if needed)
node_modules/
dist/
.DS_Store
*.log
*.pyc
__pycache__/
.env
